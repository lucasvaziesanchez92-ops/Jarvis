# =============================================================================
# Jarvis Backend - PRODUCTION requirements (Railway 512MB-1GB tier)
# =============================================================================
# Pineado a versiones exactas para builds reproducibles y rápidos.
# NO incluye TTS/STT locales (piper, onnxruntime, faster-whisper) — esas
# corren en el frontend (Web Speech API) o en un worker dedicado.
# NO incluye scikit-learn (semantic_router usa difflib de stdlib).
# NO incluye boto3 (si Railway Object Storage se necesita, se agrega aparte).
# NO incluye huggingface_hub/huggingface embeddings (chromadb usa default).
# =============================================================================

# --- LLM Agent (LangGraph stack) -------------------------------------------
langgraph==1.1.10
langgraph-prebuilt==1.0.13
langgraph-checkpoint==4.0.3
langgraph-sdk==0.3.13
langchain==1.2.14
langchain-core==1.3.2
langchain-ollama==1.0.1
langchain-openai==1.1.11
langchain-chroma==1.1.0
langchain-text-splitters==1.1.1

# --- Vector store (Wiki / Second Brain, lazy loaded) -----------------------
# langchain-chroma 1.1.0 requires chromadb>=1.3.5,<2.0.0 — pinned accordingly.
chromadb==1.4.0
lancedb==0.8.2
pyarrow
pandas==2.2.2

# --- Web framework ----------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.32.1
websockets==13.1
python-multipart==0.0.20
httpx==0.27.2

# --- Database ---------------------------------------------------------------
sqlalchemy==2.0.36
aiosqlite==0.20.0
psycopg2-binary>=2.9.9
duckduckgo-search==7.3.2
# --- Config / Validation ----------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Auth -------------------------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- Rate limit / resilience ------------------------------------------------
slowapi==0.1.9
loguru==0.7.3
tenacity==9.0.0
pybreaker==1.2.0

# --- Google APIs (Gmail, Drive, Calendar OAuth) ----------------------------
google-auth==2.37.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.160.0

# --- Groq provider ----------------------------------------------------------
groq==1.0.0

# --- Utilities --------------------------------------------------------------
python-dotenv==1.0.1
psutil==6.1.0
tiktoken==0.8.0
beautifulsoup4==4.12.3

# --- File Text Extraction ---------------------------------------------------
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5
python-frontmatter==1.1.0
